# Pinned to the versions verified against the running container on 2026-07-05.
# (Unpinned deps caused a production 500: Starlette 1.x removed the legacy
# TemplateResponse signature. Bump deliberately, then re-run portal/tests.)
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.50.0
jinja2==3.1.6
httpx==0.28.1
itsdangerous==2.2.0
python-multipart==0.0.32
